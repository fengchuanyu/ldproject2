<view>
  <input bindinput="inpHandle" placeholder="请输入聊天内容" />
</view>
<button bindtap="sendHandle">发送</button>
<view class="talk-list" wx:for="{{talkList}}" wx:key="index">
  
  <view class="question">{{item.question}}</view>
  <view class="answer">{{item.answer}}</view>
</view>
